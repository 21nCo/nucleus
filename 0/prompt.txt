should the files in @shared/data/datafn/index.ts named with .schema suffix instead of prefix?

---

What's the blast radius if we move all the contents from shared/data/datafn and shared/config into a root folder called schema?

---

perform this refactor fully

use direct root `schema/` folder and @nucleum/schema as the package name (against the existing convention - we will be moving to @nucleum in other places later)

also keep the file names short without schema. prefix like features.ts etc

---

@schema/features.ts:593 why is datafn prefixed to all of these?

@schema/features.ts:603 
@schema/features.ts:621 why are these needed?

---

They now sit in schema/features.ts inside @nucleum/schema, so datafn on the locals is redundant - remove

Drop them only if you accept a one-time reorder of nucleumDatafnSchema and a regenerate of datafn-schema.drizzle.ts.
Drop - one time reorder is fine

---

A trial generate also dropped datafnTimezoneChange, so I restored the existing generated schema.

What is this?

---

why did the new CLI gen omitted that table? what is the root cause? is it latest datafn cli code bug? or somethign else? troubleshoot

Also why still datafn prefix for some? @schema/features.ts:387

---

They still say datafn because they are the public barrel names and I did not rename the export surface.

rename these as well


also fix the datafn/CLI issue (see if latest published npm package is correct)

---

which name is better for schema directory? schema or config? or somethign else?